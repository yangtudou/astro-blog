---
import Logo from '../components/Logo.astro'
---

<header class="header-root">
  <div class="header-inner">
    <a href="/" class="logo-link" aria-label="Home">
      <Logo />
    </a>
  </div>
</header>

<style>
  .header-root {
    /* 使用 sticky 确保它在页面顶端占位，不遮挡正文第一行 */
    position: sticky;
    top: 0;
    left: 0;
    right: 0;
    width: 100%;
    z-index: 1000;

    /* 尺寸定义 */
    height: 100px;
    
    /* 基础色彩：浅色模式 */
    background-color: #ffffff;
    color: #000000; /* 这里的 color 会传给 logo-link，进而传给 Logo */
    
    display: flex;
    align-items: center;
    justify-content: center;
    
    /* 性能优化：切换主题时平滑一点 */
    transition: background-color 0.4s ease, color 0.4s ease;
  }

  /* Dark 模式适配 */
  :global(.dark) .header-root {
    background-color: #000000;
    color: #ffffff;
  }

  .header-inner {
    width: 100%;
    height: 100%;
    max-width: 1440px; /* 增加最大宽度限制，防止超大屏下视觉发散 */
    padding: 0 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .logo-link {
    display: flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    
    /* 继承 Header 的 color */
    color: inherit; 
    
    /* 交互：按压感 */
    transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* 注入样式到 Logo 组件内部 */
  .logo-link :global(svg) {
    height: 3rem; /* 调大 Logo 视觉尺寸以匹配 100px 的 Header */
    width: auto;
    /* 🔑 强制让 Logo 内部路径听从 color 变量 */
    fill: currentColor;
    stroke: none; /* 暂时屏蔽掉边框线，由 fill 决定形状 */
  }

  .logo-link:hover {
    transform: scale(1.02);
  }

  .logo-link:active {
    transform: scale(0.92);
  }

  /* 移动端适配 */
  @media (max-width: 768px) {
    .header-root {
      height: 80px;
    }
    .logo-link :global(svg) {
      height: 2.2rem;
    }
  }
</style>